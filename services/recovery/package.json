{
  "name": "@proofstack/recovery-operations",
  "version": "0.0.0",
  "private": true,
  "description": "Fail-closed backup, restore, and recovery verification operations",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "proofstack-recovery": "./dist/cli-entrypoint.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf coverage dist",
    "lint": "biome lint src",
    "recovery:database:backup": "pnpm build && node dist/cli-entrypoint.js database-backup",
    "recovery:database:restore": "pnpm build && node dist/cli-entrypoint.js database-restore",
    "test": "vitest run --coverage",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@proofstack/postgres": "workspace:*"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "catalog:",
    "@proofstack/artifacts": "workspace:*",
    "@proofstack/contracts": "workspace:*",
    "@proofstack/datasets": "workspace:*",
    "@proofstack/recovery": "workspace:*",
    "@proofstack/s3": "workspace:*",
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "pg": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
